type NearKartsSimpleBattle @entity {
  id: ID!
  timestamp: BigInt,
  homeAccount: String!,
  homeTokenId: String!,
  awayTokenId: String!,
  winner: Int!,
  battle: BigInt,
  prize: String!,
  extra: String!
}
